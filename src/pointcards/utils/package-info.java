/**
 * The `utils` package provides a collection of utility classes and methods
 * that support various functionalities across the application.
 * 
 * This package includes:
 * 
 * - {@link pointcards.utils.Randomizer}: A utility class for random operations,
 * including shuffling lists, setting seeds for reproducibility, and generating
 * random integers within specified bounds.
 * 
 * - {@link pointcards.utils.Logger}: A simple logging utility that supports
 * different log levels (DEBUG, INFO, WARN, ERROR) and color-coded log messages
 * for better readability in the console. The log level can be set using the
 * environment variable LOGGER_LEVEL.
 * 
 * These utilities are designed to be reusable and provide common functionality
 * that can be leveraged throughout the application to avoid code duplication
 * and improve maintainability.
 */
package pointcards.utils;