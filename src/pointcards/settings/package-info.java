/**
 * The `settings` package contains classes that manage the configuration and
 * settings
 * for the game. These settings include server configurations, game-specific
 * settings,
 * and optional settings that can be customized by the user.
 * 
 * This package includes:
 * 
 * - {@link pointcards.settings.ProgramSettings}: A base class for common
 * settings used by both server and client.
 * - {@link pointcards.settings.GameSettings}: A class that extends
 * `ProgramSettings` to include game-specific settings.
 * - {@link pointcards.settings.OptionalGameSettings}: A class that extends
 * `ProgramSettings` to include optional settings that can be customized.
 */
package pointcards.settings;